# OpsLens AI — Python Dependencies
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # form data support

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0               # async PostgreSQL driver
alembic>=1.13.0               # database migrations
psycopg2-binary>=2.9.9        # sync driver (for Alembic autogenerate)

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic[email]>=2.7.0        # [email] extra installs email-validator for EmailStr
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT[crypto]>=2.8.0          # RS256 + HS256 JWT validation and issuance
cryptography>=42.0.0          # AES-256 credential encryption (Fernet)

# Enterprise SSO — SAML 2.0
# Requires native libraries: libxml2-dev + libxmlsec1-dev
#   Debian/Ubuntu:  apt-get install libxml2-dev libxmlsec1-dev libxmlsec1-openssl
#   Alpine Linux:   apk add libxmlsec1-dev
#   macOS:          brew install libxmlsec1
python3-saml>=1.16.0           # SAML 2.0 SP implementation (OneLogin python3-saml)

# Enterprise SSO — LDAP / Active Directory
ldap3>=2.9.1                  # Pure-Python LDAP client (no native deps required)

# ── AI / LLM ─────────────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-ollama>=0.2.0       # on-prem / private LLM via Ollama
langchain-anthropic>=0.2.0    # Claude (enterprise / BAA) LLM provider
langchain-qdrant>=0.1.0
langchain-core>=0.2.0
langgraph>=0.2.0              # multi-step reasoning / agent orchestration
openai>=1.30.0
anthropic>=0.30.0             # Anthropic SDK (used by log_scanner direct call)
tiktoken>=0.7.0               # token counting for chunking
rank-bm25>=0.2.2              # BM25 keyword search for hybrid retrieval
cohere>=5.0.0                 # Cohere Rerank API (optional — falls back gracefully)

# ── Vector database ───────────────────────────────────────────────────────────
qdrant-client>=1.9.0

# ── Task queue ────────────────────────────────────────────────────────────────
celery[redis]>=5.4.0
redis>=5.0.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27.0                 # async HTTP (Airbyte API, SendGrid)

# ── Text processing ───────────────────────────────────────────────────────────
pdfplumber>=0.11.0            # PDF text extraction
python-docx>=1.1.0            # DOCX text extraction
markdownify>=0.13.0           # HTML → Markdown conversion

# ── Observability ─────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0
structlog>=24.0.0             # structured JSON logging

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                 # async test client
factory-boy>=3.3.0            # test fixtures
